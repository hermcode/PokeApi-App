import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { PokemonApp } from './PokemonApp';

const root = document.getElementById('root');

ReactDOM.render(<PokemonApp />, root);